@using Account.Management.Infrastructure.Extentions
@using Account.Management.Web.Areas.Admin.Models

@if (TempData.Peek<ResponseModel>("ResponseMessage") != null)
{
    var response = TempData.Get<ResponseModel>("ResponseMessage");
    <div id="response-alert" class="alert alert-@(response.Type.ToString().ToLower())" role="alert">
        @response.Message
    </div>
}
